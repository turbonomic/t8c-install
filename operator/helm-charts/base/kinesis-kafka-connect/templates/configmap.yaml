apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-config-map
data:
  {{- range $key, $val := .Values.connector.config -}}
  {{- if $val }}
  {{ upper $key }}: {{ $val | quote }}
  {{- end }}
  {{- end }}