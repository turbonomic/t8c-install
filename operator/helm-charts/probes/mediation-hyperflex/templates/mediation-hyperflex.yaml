apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-hyperflex
  name: mediation-hyperflex
spec:
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        zone: secure
        io.kompose.service: mediation-hyperflex
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: {{ template "hyperflex_image" . }}
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: JAVA_OPTS
{{- if .Values.global }}
  {{- if .Values.global.debug }}
          value: {{ .Values.global.dev_options }} {{ .Values.java.options }}
  {{- else }}
          value: {{ .Values.java.options }}
  {{- end }}
{{- else }}
          value: {{ .Values.java.options }}
{{- end }}
        - name: LOG4J_CONTEXT_SELECTOR
          value: com.vmturbo.mediation.common.context.ThreadGroupPrefixContextSelector
        - name: clustermgr_host
          value: clustermgr
        - name: clustermgr_port
          value: "8080"
        - name: clustermgr_retry_delay_sec
          value: "10"
        - name: component_type
          value: mediation-hyperflex
        - name: consul_host
          value: consul
        - name: instance_id
          value: mediation-hyperflex-1
        readinessProbe:
          httpGet:
            port: 8080
            path: /health
        resources:
{{ toYaml .Values.resources | indent 12 }}
        securityContext:
          readOnlyRootFilesystem: true
          capabilities:
            drop:
            - ALL
        volumeMounts:
        - mountPath: /tmp
          name: mediation-hyperflex-tmpfs0
{{- if .Values.global }}
  {{- if and .Values.global.registry .Values.global.imageUsername .Values.global.imagePassword }}
      imagePullSecrets:
      - name: turbocred
  {{- end -}}
{{- end }}
      restartPolicy: Always
      volumes:
      - name: mediation-hyperflex-tmpfs0
        emptyDir: {}
  selector:
    matchLabels:
      io.kompose.service: mediation-hyperflex
---
apiVersion: v1
kind: Service
metadata:
  labels:
    zone: secure
    io.kompose.service: mediation-hyperflex
  name: mediation-hyperflex
spec:
  ports:
  - name: http-mediation-hyperflex
    port: 8080
{{- if .Values.global }}
  {{- if .Values.global.debug }}
  - name: tcp-mediation-hyperflex-debug
    port: 8000
  {{- end -}}
{{- end }}
  selector:
    io.kompose.service: mediation-hyperflex
